[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm", "graphicsmagick", "ghostscript", "poppler_utils"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
